## TODO type structure decision
scalar Frequency

type Namespace {
  id: ID!
  name: String!

  namespaces: [Namespace!]
  employees: [User!]
}

type User {
  id: ID!
  name: String!
  role: String!
}

type NotificationTask {
  id: ID!
  questions: [Question!]

  divisions: [Division!]
  users: [User!]

  frequencies: [Frequency!]
}

union Question = RateQuestion | ResponseQuestion
type RateQuestion {}
type ResponseQuestion {}